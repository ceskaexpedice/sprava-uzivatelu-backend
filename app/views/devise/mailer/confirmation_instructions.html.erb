<p>Pro dokončení aktivace účtu klikněte na odkaz níže.</p>

<p><%= link_to "Aktivovat účet", confirmation_url(@resource, {confirmation_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url']}).html_safe %></p>
